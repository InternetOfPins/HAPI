# HAPI dev notes

## Type transformations

### Check

Check is a map of a transformation `X` over a tree `O` with a predicate `C`

where:

`X` high order template, unless we need to pattern match.
`C`, `O` are types

specialization will be done on `O`




