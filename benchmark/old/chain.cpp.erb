#include "hapi.h"

// generate: struct T1{}; struct T2{}; ... struct TN{};
<% (1..n).each do |i| %>
struct T<%= i %>{};
<% end %>

#ifdef METABENCH
// the thing being measured: a chain of N types
using MyChain = hapi::Chain
<%= (1..n).map { |i| "T#{i}" }.join(",\n") %>
>;
MyChain instance;
#endif