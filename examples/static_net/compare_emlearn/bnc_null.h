// the floor: no model. What every other program pays for the harness, the row fetch and the call; run.sh subtracts it.
#include <stdint.h>
__attribute__((noinline)) bool bnc_predict(const uint8_t* x){ return x[0]>127; }
