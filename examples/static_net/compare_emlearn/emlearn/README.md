The emlearn C headers the comparison compiles against, copied unchanged from the `emlearn` 0.23.2 Python package (MIT, Jon Nordby; LICENSE.md next to
this file), so `run.sh` needs only avr-gcc and simavr. The trees are self-contained C; only the MLP (`eml_net`) needs these.
